# Variáveis de Ambiente - Template
# Copie este arquivo para .env e preencha com os valores reais
# IMPORTANTE: O arquivo .env não será commitado no Git (está no .gitignore)

# Chave secreta do Flask para sessões e segurança
# Gere uma nova chave com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=sua_chave_secreta_aqui
